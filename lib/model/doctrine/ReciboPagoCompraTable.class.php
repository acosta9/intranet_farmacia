<?php

/**
 * ReciboPagoCompraTable
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 */
class ReciboPagoCompraTable extends Doctrine_Table
{
    /**
     * Returns an instance of this class.
     *
     * @return object ReciboPagoCompraTable
     */
    public static function getInstance()
    {
        return Doctrine_Core::getTable('ReciboPagoCompra');
    }
    public static function doSelectJoinOtros($query) {
        $rootAlias = $query->getRootAlias();
  
        return $query->select($rootAlias.'.*, p.full_name as pname, fp.acronimo as fpago, cp.id as cpid,
        f.id as fid, f.num_factura as fnum, 
        fg.id as fgid, fg.num_factura as fgnum,
        e.id as eid, e.nombre as ename, e.acronimo as emin,
        b.nombre as bnombre,
        LPAD(ncontrol, 6, 0) as ncontrol, CAST(ncontrol AS UNSIGNED) as ninteger, FORMAT(REPLACE(monto, " ", ""), 4, "de_DE") as monto')
          ->leftJoin($rootAlias . '.Banco b')
          ->leftJoin($rootAlias . '.Empresa e')
          ->leftJoin($rootAlias . '.Proveedor p')
          ->leftJoin($rootAlias . '.FormaPago fp')
          ->leftJoin($rootAlias . '.CuentasPagar cp')
          ->leftJoin('cp.FacturaCompra f')
          ->leftJoin('cp.FacturaGastos fg');
    }
}